<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd">

    <!--配置不拦截资源-->
    <security:http pattern="/jsp/login.jsp" security="none"/>
    <security:http pattern="/jsp/failer.jsp" security="none"/>
    <security:http pattern="/css/**" security="none"/>
    <security:http pattern="/img/**" security="none"/>
    <security:http pattern="/plugins/**" security="none"/>
    <security:http pattern="/bootstrap/**" security="none"/>
    <security:http pattern="/fonts/**" security="none"/>

    <!--配置拦截规则-->
    <security:http auto-config="true" use-expressions="true">
        <!--配置要拦截的url 和该url所需要具备的角色（权限）-->
        <security:intercept-url pattern="/**" access="hasAnyRole('ROLE_3')"/>
        <!--定义权限校验后，需要跳转的页面-->
        <security:form-login
                username-parameter="username"
                password-parameter="userpass"
                login-page="/jsp/login.jsp"
                login-processing-url="/login.do"
                default-target-url="/index.jsp"
                authentication-failure-url="/jsp/failer.jsp"
                authentication-success-forward-url="/jsp/main.jsp"
        />

        <!-- 关闭跨域请求 -->
        <security:csrf disabled="true"/>
        <!-- 退出 -->
        <security:logout invalidate-session="true" logout-url="/logout.do" logout-success-url="/jsp/login.jsp" />

    </security:http>

    <!--配置用户名和密码的权限  从数据库拿取-->
    <!-- 切换成数据库中的用户名和密码 -->
    <security:authentication-manager>
        <security:authentication-provider user-service-ref="userService">
            <!-- 配置加密的方式-->
            <!--<security:password-encoder ref="passwordEncoder"/>-->
        </security:authentication-provider>
    </security:authentication-manager>

    <!-- 配置加密类 -->
    <!--<bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>-->

</beans>